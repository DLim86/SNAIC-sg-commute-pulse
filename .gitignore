# API keys — never commit
config.py

# Google Calendar OAuth2 — never commit
credentials.json
token.json

# Registration documents and credential files (contain real API keys / tokens)
LTA/
OneMap/
Prompt.txt
Google Calendar/

# Database
*.duckdb
*.duckdb.wal

# ML model artifacts (regenerated by scripts/model.py --train)
models/*.pkl

# Environment
.env
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Raw API data (regenerated each pipeline run)
data/raw/

# Project documents (may contain course content — keep local only)
project_instructions.pdf

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
.idea/
*.swp
